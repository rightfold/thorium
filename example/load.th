CREATE INPUT STREAM in double precision;
CREATE OUTPUT STREAM out double precision;
CREATE REACTOR load_warnings AS
    FROM in
    DISTINCT in WITHIN PERIOD timeout
    SELECT in INTO OUTPUT STREAM out;
